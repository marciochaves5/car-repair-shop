# Where the .NET API is reachable. Used by the Vite dev proxy (see vite.config.js).
# Check Properties/launchSettings.json in the API project for the actual port.
VITE_API_TARGET=https://localhost:62635
